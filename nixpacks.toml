[phases.setup]
nixPkgs = ["php", "composer", "git", "nodejs", "npm"]

[phases.install]
cmds = ["composer install --no-interaction --prefer-dist --optimize-autoloader", "npm install && npm run build"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"